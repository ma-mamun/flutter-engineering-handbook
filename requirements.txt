# Documentation toolchain for the Flutter Engineering Handbook.
# Install with:  pip install -r requirements.txt
# Serve with:    mkdocs serve

mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-minify-plugin==0.8.0

# Pygments is pinned alongside pymdown-extensions on purpose. Pygments 2.19
# started rejecting the `filename=None` that older pymdown-extensions releases
# pass for every highlighted block, which fails the build with a bare
# "'NoneType' object has no attribute 'replace'". 11.0 fixes it; keep these two
# in step when upgrading either.
pymdown-extensions==11.0.1
pygments==2.21.0
